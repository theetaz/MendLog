import { ComingSoon } from '../src/features/placeholder/ComingSoon';

export default function NewJobModal() {
  return (
    <ComingSoon
      title="New job"
      blurb="Voice-first capture — record in Sinhala or English, add photos, AI fills the form. Slice 3."
    />
  );
}
